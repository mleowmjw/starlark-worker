package main

import "fmt"

func main() {
        fmt.Println("Welcome to simple session!!")

// Overall workflow for the user + day? Index of usage? Is like Cursor agent?
// Simple session will have only one task running per user?
// Starts a session; based on the agent name + session / request?
// Detects intent then kicks off or continues session?
}

