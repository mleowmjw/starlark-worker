# AGENTS

Ignore from context .. all skills to run manually

## Integration Tests

- Using Temporal testsuite and components tested via anonymous function method
 replacement techniques

## End-to-End Tests

- Has real standalone; can use the built in server no need run the temporal-cli via overmind.  
- Can still have options to mock up certain compinents via anonymous function method replacement techniques
- Which components are mocked or using real; are determined via config.yaml file by default
- Can override config via passing in

## Final Manual Acceptance Tests

- No mocks; use mise task - mise uat
- temporal-cli run via overmind


All commands are incorporated in mise tasks ..
